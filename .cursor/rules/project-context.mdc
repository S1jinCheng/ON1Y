---
description: On1y 项目背景、环境与开发约定（Windows D:\On1y）
alwaysApply: true
---

# On1y 项目上下文

## 环境与路径

- **唯一开发路径**：`D:\On1y`（Windows 原生，不再使用 WSL `/home/sijin/On1y`）
- **Python**：Conda 环境 `on1y`（Python 3.11+）
- **GitHub**：https://github.com/S1jinCheng/ON1Y
- **Git 身份**：`S1jinCheng <Chengsijin815@gmail.com>`
- **配置**：`D:\On1y\.env`（不进 Git）；数据在 `D:\On1y\data\`

## 日常命令

```powershell
conda activate on1y
cd D:\On1y
on1y serve                          # http://127.0.0.1:8765
on1y cookies status
on1y subscriptions --platform all --ingest   # bilibili + youtube/zhihu RSS
on1y hotlist sync                            # 知乎热榜（独立于 zhihu 关注）
on1y search -q "关键词"
on1y search rebuild
```

前端（另开终端）：`cd D:\On1y\frontend && npm run dev`

## 架构概览

个人知识库：**采集 → 提取正文/字幕 → LLM 摘要分类 → 全文检索 → Web 工作台**

| 模块 | 路径 |
|------|------|
| CLI / 服务 | `on1y/cli/main.py`, `on1y/web/app.py` |
| 存储 | `on1y/adapters/sqlite_storage.py`, `data/on1y.db` |
| 订阅编排 | `on1y/subscriptions/`（B 站 API + YT/知乎 RSS + sync_job） |
| B 站订阅 | `on1y/ingestion/bilibili_subscriptions.py` |
| 全文搜索 | `on1y/search/fts.py`（FTS5 trigram + BM25，短词 LIKE 兜底） |
| LLM 摘要 | `on1y/distill/`, `auto_distill_after_subtitles=True` |
| 浏览器爬取 | `on1y/browser/`, Playwright（优先 Chrome/Edge channel） |
| 前端 | `frontend/` Next.js 工作台 |

## 已完成功能（勿随意回退）

1. **B 站 UP 订阅**：默认 `bilibili_up_poll_mode=dynamic`（关注动态 `type=video`），跳过图文/专栏/广告；不要用 per-UP `space/arc/search` 除非用户明确要求（易 412 限流）。
2. **FTS 搜索（硬性需求）**：schema v6 + `knowledge_fts`；入库/摘要/标签变更自动索引；前端搜索框 300ms 防抖 + 高亮。
3. **LLM 摘要**：`auto_distill_after_subtitles=True`；`/api/distill/backfill` 批量补摘要。
4. **Cookie**：YouTube / Bilibili / 知乎已有；路径 `data/cookies/*.json`。
5. **统一订阅**：`on1y subscriptions --platform {bilibili|youtube|zhihu|all}`；`zhihu` **不含**热榜；`sync_since` 三平台均生效（RSS 按发布时间过滤）。
6. **可选定时**：`ON1Y_AUTO_SYNC_ENABLED=true` 时 `on1y serve` 后台周期同步。

## 数据库与配置

- SQLite：`data/on1y.db`（schema v6，含 FTS 索引 ~800+ 文档）
- 订阅设置：`data/subscription_settings.json`
- Cookie：`data/cookies/`（`.gitignore` 排除）
- 敏感项：`.env`、LLM API Key、Cookie 文件 **禁止提交 Git**

## 开发约定

- 最小改动 diff；匹配现有代码风格；不 over-engineer
- 只有用户明确要求才 `git commit` / `git push`
- B 站订阅功能默认保持 **enabled**；用户要的是「能跑」，不是静默关闭
- Windows 上 Playwright 用系统 Chrome/Edge；Cookie 用 `python scripts/export_cookies.py <platform>`
- push 若失败：`git config --global http.version HTTP/1.1`，或 GitHub Desktop + VPN

## 常见问题

| 现象 | 处理 |
|------|------|
| 8765 端口占用 | `netstat -ano \| findstr :8765` → `taskkill /PID <pid> /F` |
| `not a git repository` | 在 `D:\On1y` 执行 `git init` 并 `git remote add origin ...` |
| WSL 与 Windows 文件不同步 | 只改 `D:\On1y`，勿再编辑 WSL 副本 |
